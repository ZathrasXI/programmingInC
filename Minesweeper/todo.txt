TODO: 
- download files from Neill's repo and compile and run ms.c with those
- check with valgrind
- compile with clang and gcc
- compile with `gcc msc. drv.c -g3 -fsanitize=address -fsanitize=undefined -o ms_val`

